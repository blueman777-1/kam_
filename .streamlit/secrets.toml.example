# 이 파일을 secrets.toml 로 복사한 뒤 값을 채우세요.
# secrets.toml 은 .gitignore 에 있어 커밋되지 않습니다.

OPENDART_API_KEY = "여기에 OpenDART 인증키 40자리"

# 선택: 없으면 AI 요약만 비활성화되고 원문 조회는 정상 동작합니다.
GEMINI_API_KEY = "여기에 Google AI Studio 키"

# 선택: 해외 리전(Streamlit Cloud)에 배포할 때만 필요합니다.
# 서울 리전에 올린 중계 주소. 자세한 내용은 api/README.md 참고.
# DART_PROXY_BASE = "https://<프로젝트>.vercel.app"
